<%= render 'shared/header' %>
<div class='container mx-auto'>
  <div class='flex flex-col mx-auto w-full bg-white py-4 text-gray-600 rounded shadow-2xl'>
  <h1 class='title'>ユーザー一覧</h1>
      <%= render @users %>
      <%= paginate @users %>
  </div>
</div>
